# uv-managed virtual environment (recreate with `uv sync`)
.venv/

# notebook figure outputs (regenerated by running the notebooks)
figures/

# python caches
__pycache__/
*.pyc

# regenerable intermediate (rebuild with `python -m hierarchical.beta.precompute_mixture_tables`)
hierarchical/beta/p_e_mixture_tables.pkl
